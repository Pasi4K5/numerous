name: .NET Setup

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: .NET Setup
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 8.0
